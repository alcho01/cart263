//The universal class will be used to extend dimensions and position when needed.
class Universal {
  constructor(w, h, x, y) {
    //dimensions
    this.width = w;
    this.height = h;
    //position
    this.x = x;
    this.y = y;
  }

  display() {
    //Nothing to display 
  }
}
